name: third party workflow
on: push

jobs:
    asci_job:
        runs-on: ubuntu-latest
        steps:
         - name: checkout repo
           uses: actions/checkout@v4
         - name: excute shell script
           run: |
             chmod +x ascii-script.sh
             ./ascii-script.sh
        #  - name: install cowsay 
        #    run: sudo apt-get install cowsay -y  

        #  - name: excecute cowsay cmd
        #    run: cowsay -f dragon "run for dragon i m running " >>dragon.txt

        #  - name: test file 
        #    run: grep -i "dragon" dragon.txt
    
        #  - name: read file 
        #    run: cat dragon.txt

         - name: list file 
           run: ls
